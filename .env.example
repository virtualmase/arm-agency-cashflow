NODE_ENV=development
PORT=3000
CANONICAL_ORIGIN=https://arm-agency.xyz

# Obtain restricted/test credentials from the Stripe dashboard. Never commit keys.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Generate a long random value and send it as `Authorization: Bearer <value>`.
SCHEDULED_JOB_SECRET=

# Keep false until a provider confirms delivery before sequence state is advanced.
EMAIL_DELIVERY_ENABLED=false

DATABASE_URL=
