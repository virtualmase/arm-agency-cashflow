# Recent Browser Validation Notes

| Route | Result | Interpretation |
| --- | --- | --- |
| `/insights/ai-infrastructure-audit` | Direct route rendered the guide title, decision framing, readiness questions, scoped deliverables, and diagnostic CTA. | The new public authority-guide route renders correctly for an unauthenticated visitor. |
| `/admin` | Development-session request showed the authentication-required state and sign-in control. | The owner dashboard remains protected from unauthenticated browser access. The decision-log interface is covered by type checks, production build, and admin-procedure unit tests; visual authenticated review can occur when an owner session is available. |
| `/insights/image-seo-fundamentals` | Direct route rendered the source-backed visual-discovery guide, optimized-image fundamentals, conversion CTA, and three authoritative sources. | The new public image SEO resource is readable, linked to a qualified next step, and avoids unsupported discovery or ranking guarantees. |
| `/insights/image-seo-fundamentals` (visual revision) | The original 1200×630 SVG process map rendered in the guide with descriptive alt text, an explanatory caption, and no overlap with the page narrative. | The first public image SEO asset is meaningful rather than decorative, is visible in its claimed page context, and is ready for the associated preview metadata validation. |
| `/insights/image-seo-fundamentals` (metadata inspection) | The rendered head exposes `og:image`, `og:image:alt`, and `twitter:image` pointing to the visible SVG; the `<img>` reports contextual alt text and `width="1200" height="630"`. | Preview metadata, accessible markup, and intrinsic dimensions align with the actual visible asset rather than describing a generic or hidden image. |
| `/insights/image-seo-fundamentals` (post-code-splitting) | The public guide retained its full SSR content, original visual, caption, CTA, and sources after admin-only workspace code was split from the initial client bundle. | Private analytics code splitting did not regress public authority content or its crawler-readable route. |
| `/portal` (post-code-splitting) | An unauthenticated request rendered the Client Portal sign-in state and did not expose purchases, subscriptions, or invoices. | Client account data remains protected after the bundle change. |
| `/admin` (post-code-splitting) | An unauthenticated request rendered only the authentication-required state and sign-in control. | Owner-only dashboard data and decision controls remain protected after the bundle change. |
| `/#pricing` (visual-discovery conversion path) | The Swell GEO offer rendered both its operating-guide and visual-discovery-guide links, while the contact form included an “Image SEO & Visual Discovery Audit” qualification option. | The new bounded audit is discoverable from the relevant revenue stream and can enter the existing qualified lead-capture workflow without an unsupported self-serve purchase flow. |
| `/#pricing` (claim-safety revision) | The GEO packages displayed bounded content, structured-data, public-surface, and review-cadence language; the contact section stated that scope, timing, participants, and pilot terms are confirmed during qualification. | The visible offer copy preserves a clear conversion path while removing unsupported uptime, citation, fault-tolerance, and outcome commitments. |
| `/satisfaction` (feedback-integrity revision) | An unauthenticated request rendered only the sign-in state and exposed no feedback aggregates, names, or comments. | The employee feedback surface retains its privacy boundary while the authenticated owner view now derives metrics from submitted feedback rather than static telemetry. |
| `https://arm-agency.xyz/` (controlled-launch domain gate) | HTTPS served a public ARM site, but the rendered content was an outdated deployment containing unsupported customer-count, agent-count, uptime, SLA, citation, performance, and guarantee claims removed from the current validated release. | **Launch blocked.** The primary domain is available but is not serving the current evidence-safe release; do not begin promotion, change canonical assumptions, or initiate payment validation. |
| `https://www.arm-agency.xyz/` (controlled-launch redirect gate) | The www host redirected to `https://arm-agency.xyz/` and served the same outdated deployment. | Redirect behavior is consistent, but the current primary target remains unsuitable for launch until the current validated release is published. |
| `https://arm-agency.xyz/` (post-deployment verification) | The primary domain now renders the current evidence-safe homepage, including the updated headline and visual-discovery conversion path. | Domain deployment identity is now current; proceed with technical gates rather than promotion. |
| `https://arm-agency.xyz/` (canonical gate) | The rendered canonical and Open Graph URL still point to `https://armcashflow-gw96qvq2.manus.space/`; the controlled launch verifier failed only this canonical-host check. | **Launch remains blocked.** Update primary-host metadata and discovery artifacts to `https://arm-agency.xyz`, then redeploy and reverify before promotion. |
| `https://arm-agency.xyz/` (post-metadata-release canonical check) | The current public application release is live, but the rendered canonical, Open Graph URL, and Organization schema still use the temporary deployment host. | **Launch remains blocked.** The `CANONICAL_ORIGIN` runtime configuration is overriding the new code default and must be updated to `https://arm-agency.xyz`, then revalidated. |
| `https://arm-agency.xyz/` (post-runtime-fix canonical check) | The live page now declares `https://arm-agency.xyz/` as canonical and Open Graph URL; its Organization schema uses `https://arm-agency.xyz/#organization` and contains no temporary-host reference. | Primary-domain metadata identity passes. Proceed to public-discovery, privacy-boundary, and offer-routing gates. |
| `https://arm-agency.xyz/faq.html` (public discovery gate) | The standalone FAQ loaded on the primary domain with its current bounded-service, customer-access, and no-guarantee content. | The primary public FAQ surface is reachable and consistent with the evidence-safe release. |
| `https://arm-agency.xyz/portal` (private-route HTTP gate) | The unauthenticated response returned `X-Robots-Tag: noindex, follow`, a restrictive baseline CSP, and a no-store cache policy. Its initial response body did not expose account-summary, subscription, purchase, or sign-in-content strings. | The server-side portal shell maintains crawler and cache protections; authenticated portal rendering and Stripe ownership tests remain separately covered by the existing regression suite. |
| `https://arm-agency.xyz/api/trpc/stripe.createCheckout` (pre-deployment security probe) | An unauthenticated request returned a live Stripe Checkout URL. The session was not completed or paid. | **Launch blocked at the time of the probe.** The live runtime was serving stale checkout authorization behavior and required a fresh deployment before any promotion or payment validation. |
| `https://arm-agency.xyz/api/trpc/stripe.createCheckout` (post-deployment security probe) | After deployment success was reported, the same unauthenticated request returned HTTP 401 and no Checkout URL. | The primary runtime now rejects anonymous checkout creation before it can open a payment session. The remaining authenticated return-route and lead-capture checks require an owner-authorized live operational test. |
| `pnpm verify:launch -- https://arm-agency.xyz --canonical https://arm-agency.xyz` (post-deployment technical gate) | All public-route, canonical, discovery-file, private-route noindex, CSP, and security-header checks passed. | The technical primary-domain launch verifier is clear. Do not begin promotion until the remaining capacity/owner approval and authorized operational routing checks are completed. |
| `/insights/technical-seo-ai-discovery` (authority-silo addition) | The new server-rendered guide displayed its responsibility-map decision framing, four source-linked technical sections, bounded contact route, primary-source reference list, and related links to the AI Discovery hub, structured-data guide, and GEO readiness guide. | The first new AI Discovery Operating System spoke is public, readable, citation-backed, and connected to the next relevant decisions without visibility guarantees. |
| `/insights/structured-data-governance` (authority-silo addition) | The new server-rendered guide displayed visible-content alignment, governance, eligibility-boundary guidance, section-level Google citations, an external source list, and reciprocal links to the hub, technical SEO, and evidence-led content pages. | Structured-data education is discoverable as a distinct, evidence-safe topic cluster and avoids unsupported rich-result or AI-visibility promises. |
| `/insights/evidence-led-content-architecture` (authority-silo addition) | The new server-rendered guide displayed decision-led editorial guidance, section-level Google and web.dev citations, a visible reference section, and connected reading paths to the hub, image SEO, and GEO readiness. | The public content system now explains its own source, claim, and internal-linking discipline without manufacturing proof or promising external outcomes. |
| `/insights/ai-discovery-readiness` (authority-silo hub update) | The existing hub displayed five connected next-decision paths: technical SEO, structured-data governance, evidence-led content, GEO readiness, and image SEO fundamentals. | The hub-and-spoke model is visible to public readers and directs each path toward a bounded, qualified next step. |
| `http://127.0.0.1:3000/` (security-header diagnosis) | The direct application response returned `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, and the baseline CSP. The development preview proxy omitted `X-Frame-Options`, causing the local proxy launch-verifier failure. | The reported local verifier exception is a preview-proxy behavior, not a missing application header. The production primary-domain verifier previously passed the required header checks. |
| `/insights` (authority-library index) | The server-rendered library page displayed three decision-led groups—AI Discovery Operating System, Accountable AI Operations, and Operator and Network Decisions—with all ten published guide cards, direct internal links, visibility-boundary language, and a qualified contact route. | The public content system now has a readable, indexable entry point that lets visitors navigate the authority silo by operating decision rather than by a generic blog archive. |
| `/` (homepage authority-system module) | The public landing page displayed the AI Discovery Operating System module after core capabilities, with an evidence-safe blue-ocean position, a link to the authority library, and distinct internal links to technical SEO, structured-data governance, and evidence-led content guides. | The authority silo is now discoverable from the primary conversion page as well as the navigation and library index, while retaining clear boundaries against external-platform visibility promises. |
